# Plan: Default to Active Status Only

## Changes

### 1. `app/(dashboard)/receivings/page.tsx`
- Update initial `getReceivings()` call to pass `{ status: "Active" }`
- Update `handleServerSearchClear` to pass `{ status: "Active" }` instead of empty

### 2. `app/(dashboard)/releasings/page.tsx`
- Update initial `getReleasings()` call to pass `{ status: "Active" }`
- Update `handleServerSearchClear` to pass `{ status: "Active" }` instead of empty
