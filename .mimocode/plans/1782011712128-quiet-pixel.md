# Plan: Fix auth-service.ts stale status_id reference

## Bug
`lib/services/auth-service.ts` lines 34-36 and 43-44 still reference the old `status_id` pattern:
- `.populate("status_id", "status")` — field no longer exists on User schema
- `const status = user.status_id as unknown as { status: string }` — treats it as a populated document
- `if (status.status !== "Active")` — would fail at runtime

## Fix

### `lib/services/auth-service.ts`
- **Line 34-37**: Remove `.populate("status_id", "status")` from the query
- **Lines 43-46**: Replace with `if (user.status !== "Active")`

## Verification
1. TypeScript: `npx tsc --noEmit`
2. Lint: `npm run lint`
