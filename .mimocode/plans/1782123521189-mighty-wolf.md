# Plan: Item View Modal — Full-width "Name" and "Image URL"

## Current layout
The first section uses `grid grid-cols-2` with "Name" in the left column and "Image URL" in the right column, both constrained to 50% width.

## Target layout
Move "Name" and "Image URL" to span the full modal width, above/below the 2-column grid.

### `components/modals/item-view-modal.tsx`

1. Move the "Name" div out of the left column to a new full-width div above the `grid grid-cols-2` in the first section (line 71).
2. Move the "Image URL" div out of the right column to a new full-width div below the `grid grid-cols-2` in the first section.
3. Remove the now-empty grid columns if they become sparse, or leave remaining fields in the 2-col grid.

Result structure:
```
<div className="rounded-xl bg-muted/30 p-4">
  <div>                              ← full-width Name
  <div className="grid grid-cols-2"> ← Item Code, Category, Brand, Model, Description, UOM, Min Stock
  <div>                              ← full-width Image URL
</div>
```

## Verification
1. Open Items page → View Details → Name and Image URL should span full width
2. `npx eslint components/modals/item-view-modal.tsx`
