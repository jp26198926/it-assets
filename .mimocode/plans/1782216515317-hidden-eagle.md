# Plan: Edit Asset Modal Changes

## File to modify
`components/modals/asset-form-modal.tsx`

## Changes

### 1. Barcode field — make non-editable (line 193-202)
Currently the barcode `<Input>` has `disabled={!asset}` (disabled during create, editable during edit). Change to `disabled` always — the barcode should never be editable regardless of mode.

```tsx
// Line 193: Change disabled prop
disabled
// Remove: disabled={!asset}
// Keep the placeholder and value logic as-is
```

### 2. Remove "Assigned to (Employee)" and "Assigned to (Department)" fields (lines 317-382)
Delete the entire `{asset && (...)}` block that renders the assigned_to_employee and assigned_to_department Select fields.

Also remove the now-unused state and code:
- Remove `employees` and `departments` state variables (lines 60-65)
- Remove their loading from `getAssetSelectOptions()` (lines 75-76)
- Remove `assigned_to_employee` and `assigned_to_department` from `defaultFormData` (lines 43-44)
- Remove their population in the edit `useEffect` (lines 101-102)
- Remove their spread in `handleSubmit` (lines 132-133)

## Verification
- Run `npm run build` to confirm no type errors
- Open the Assets page, click Edit on an asset, confirm:
  - Barcode field is grayed out / non-editable
  - Assigned To (Employee) and Assigned To (Department) fields are gone
