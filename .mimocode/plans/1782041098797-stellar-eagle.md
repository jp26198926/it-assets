# Hide Assignment Fields on Add New Asset

## Change
In `components/modals/asset-form-modal.tsx`, wrap the "Assigned To (Employee)" and "Assigned To (Department)" row (lines 232–272) in a conditional that only renders when editing an existing asset (`asset` prop is non-null).

## File
- `components/modals/asset-form-modal.tsx` — add `{asset && ( ... )}` around the assignment fields grid

## Verification
- Open "Add New Asset" modal — assignment fields should not appear
- Open "Edit Asset" modal — assignment fields should appear
