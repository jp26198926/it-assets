# Plan: Ticket Detail Page Modals Dropdown Improvements

## Summary
Update the "Edit Ticket", "Update Ticket Status", and "Assign Ticket" modals to use SearchableSelect for all dropdowns. Options are already sorted at the database level.

## Files to Modify

### 1. `components/modals/ticket-edit-modal.tsx`
**Changes:**
- Import `SearchableSelect` component
- Replace four `Select` dropdowns with `SearchableSelect`:
  - "Category" dropdown (line 90-101)
  - "Department" dropdown (line 106-121)
  - "Asset" dropdown (line 126-141)
  - "Priority" dropdown (line 146-156) - fixed list
- Remove unused `Select` imports

### 2. `components/modals/ticket-update-status-modal.tsx`
**Changes:**
- Import `SearchableSelect` component
- Replace one `Select` dropdown with `SearchableSelect`:
  - "Status" dropdown (line 67-78) - fixed list
- Remove unused `Select` imports

### 3. `components/modals/ticket-assign-modal.tsx`
**Changes:**
- Import `SearchableSelect` component
- Replace one `Select` dropdown with `SearchableSelect`:
  - "Assign To" dropdown (line 67-79)
- Remove unused `Select` imports

## Detailed Changes

### Step 1: Update `components/modals/ticket-edit-modal.tsx`

1. **Replace Select import with SearchableSelect:**
   - Remove: `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";`
   - Add: `import { SearchableSelect } from "@/components/ui/searchable-select";`

2. **Replace "Category" dropdown (lines 90-101):**
   - Replace the `Select` component with `SearchableSelect`

3. **Replace "Department" dropdown (lines 106-121):**
   - Replace the `Select` component with `SearchableSelect`

4. **Replace "Asset" dropdown (lines 126-141):**
   - Replace the `Select` component with `SearchableSelect`
   - Map assets to include barcode and itemName in name for display

5. **Replace "Priority" dropdown (lines 146-156):**
   - Replace the `Select` component with `SearchableSelect`
   - Use static options array

### Step 2: Update `components/modals/ticket-update-status-modal.tsx`

1. **Replace Select import with SearchableSelect:**
   - Remove: `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";`
   - Add: `import { SearchableSelect } from "@/components/ui/searchable-select";`

2. **Replace "Status" dropdown (lines 67-78):**
   - Replace the `Select` component with `SearchableSelect`
   - Use static options array from `statusOptions`

### Step 3: Update `components/modals/ticket-assign-modal.tsx`

1. **Replace Select import with SearchableSelect:**
   - Remove: `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";`
   - Add: `import { SearchableSelect } from "@/components/ui/searchable-select";`

2. **Replace "Assign To" dropdown (lines 67-79):**
   - Replace the `Select` component with `SearchableSelect`
   - Map users to include "Unassigned" option

## Verification

1. Open a Ticket detail page
2. Click "Edit" button
3. Verify:
   - "Category" dropdown is searchable (type to filter)
   - "Category" options are sorted alphabetically
   - "Department" dropdown is searchable (type to filter)
   - "Asset" dropdown is searchable (type to filter)
   - "Priority" dropdown is searchable
4. Click "Status" button
5. Verify:
   - "Status" dropdown is searchable
6. Click "Assign" button
7. Verify:
   - "Assign To" dropdown is searchable (type to filter)
   - "Assign To" options are sorted alphabetically
