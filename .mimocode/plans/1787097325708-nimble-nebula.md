# Plan: Transfers Page Dropdown Improvements

## Summary
Update the "Add New Transfer" modal and "Advanced Search" modal to use SearchableSelect for all dropdowns and sort them alphabetically.

## Files to Modify

### 1. `components/modals/transfer-form-modal.tsx`
**Changes:**
- Import `SearchableSelect` component
- Replace two `Select` dropdowns with `SearchableSelect`:
  - "From Location" dropdown (line 149-171)
  - "To Location" dropdown (line 178-200)
- Remove unused `Select` imports
- Sort locations alphabetically when fetched

### 2. `components/data-table/transfer-advanced-search-dialog.tsx`
**Changes:**
- Import `SearchableSelect` component
- Replace three `Select` dropdowns with `SearchableSelect`:
  - "Status" dropdown (line 111-124) - fixed list
  - "From Location" dropdown (line 130-145)
  - "To Location" dropdown (line 149-164)
- Remove unused `Select` imports
- Sort locations alphabetically when fetched

## Detailed Changes

### Step 1: Update `components/modals/transfer-form-modal.tsx`

1. **Replace Select import with SearchableSelect:**
   - Remove: `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";`
   - Add: `import { SearchableSelect } from "@/components/ui/searchable-select";`

2. **Sort locations alphabetically (line 54-55):**
   ```tsx
   setLocations(data.map((l: { id: string; name: string }) => ({ id: l.id, name: l.name })).sort((a, b) => a.name.localeCompare(b.name)));
   ```

3. **Replace "From Location" dropdown (lines 149-171):**
   - Replace the `Select` component with `SearchableSelect`

4. **Replace "To Location" dropdown (lines 178-200):**
   - Replace the `Select` component with `SearchableSelect`

### Step 2: Update `components/data-table/transfer-advanced-search-dialog.tsx`

1. **Replace Select import with SearchableSelect:**
   - Remove: `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";`
   - Add: `import { SearchableSelect } from "@/components/ui/searchable-select";`

2. **Sort locations alphabetically (line 47-48):**
   ```tsx
   setLocations(data.map((l: { id: string; name: string }) => ({ id: l.id, name: l.name })).sort((a, b) => a.name.localeCompare(b.name)));
   ```

3. **Replace "Status" dropdown (lines 111-124):**
   - Replace the `Select` component with `SearchableSelect`
   - Use static options array

4. **Replace "From Location" dropdown (lines 130-145):**
   - Replace the `Select` component with `SearchableSelect`

5. **Replace "To Location" dropdown (lines 149-164):**
   - Replace the `Select` component with `SearchableSelect`

## Verification

1. Open the Transfers page
2. Click "Add New Transfer" button
3. Verify:
   - "From Location" dropdown is searchable (type to filter)
   - "From Location" options are sorted alphabetically
   - "To Location" dropdown is searchable (type to filter)
   - "To Location" options are sorted alphabetically
4. Click "Edit" on an existing transfer
5. Verify same behavior in edit mode
6. Click "Advanced Search" button
7. Verify:
   - "Status" dropdown is searchable
   - "From Location" dropdown is searchable (type to filter)
   - "From Location" options are sorted alphabetically
   - "To Location" dropdown is searchable (type to filter)
   - "To Location" options are sorted alphabetically
