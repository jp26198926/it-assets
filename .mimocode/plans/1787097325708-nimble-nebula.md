# Plan: Assignments Page Dropdown Improvements

## Summary
Update the "Add Assignment" modal and "Advanced Search" modal to use SearchableSelect for all dropdowns. Options are already sorted at the database level.

## Files to Modify

### 1. `components/modals/assignment-form-modal.tsx`
**Changes:**
- Import `SearchableSelect` component
- Replace three `Select` dropdowns with `SearchableSelect`:
  - "Employee" dropdown (line 325-349)
  - "Department" dropdown (line 354-377)
  - "Location" dropdown (line 402-419)
- Remove unused `Select` imports
- Note: Asset dropdown already has custom search functionality, keep as is

### 2. `components/data-table/assignment-advanced-search-dialog.tsx`
**Changes:**
- Import `SearchableSelect` component
- Replace one `Select` dropdown with `SearchableSelect`:
  - "Status" dropdown (line 81-89) - fixed list
- Remove unused `Select` imports

## Detailed Changes

### Step 1: Update `components/modals/assignment-form-modal.tsx`

1. **Replace Select import with SearchableSelect:**
   - Remove: `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";`
   - Add: `import { SearchableSelect } from "@/components/ui/searchable-select";`

2. **Replace "Employee" dropdown (lines 325-349):**
   - Replace the `Select` component with `SearchableSelect`
   - Keep the onValueChange handler to set department_id

3. **Replace "Department" dropdown (lines 354-377):**
   - Replace the `Select` component with `SearchableSelect`

4. **Replace "Location" dropdown (lines 402-419):**
   - Replace the `Select` component with `SearchableSelect`

### Step 2: Update `components/data-table/assignment-advanced-search-dialog.tsx`

1. **Replace Select import with SearchableSelect:**
   - Remove: `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";`
   - Add: `import { SearchableSelect } from "@/components/ui/searchable-select";`

2. **Replace "Status" dropdown (lines 81-89):**
   - Replace the `Select` component with `SearchableSelect`
   - Use static options array

## Verification

1. Open the Assignments page
2. Click "Add Assignment" button
3. Verify:
   - "Employee" dropdown is searchable (type to filter)
   - "Employee" options are sorted alphabetically
   - "Department" dropdown is searchable (type to filter)
   - "Department" options are sorted alphabetically
   - "Location" dropdown is searchable (type to filter)
   - "Location" options are sorted alphabetically
4. Click "Edit" on an existing assignment
5. Verify same behavior in edit mode
6. Click "Advanced Search" button
7. Verify:
   - "Status" dropdown is searchable
