# Ticket Detail Page Mobile Improvements

## File to Modify
- `app/(dashboard)/tickets/[id]/page.tsx`

## Changes

### 1. Back button (mobile only)
- Import `ArrowLeft` from lucide-react
- Add a back button before the ticket title, visible only on mobile (`lg:hidden`):
```tsx
<Button
  variant="ghost"
  size="sm"
  className="lg:hidden gap-1 mb-4"
  onClick={() => router.push("/tickets")}
>
  <ArrowLeft className="h-4 w-4" />
  Back
</Button>
```
- Place it right after `<PageGuard pagePath="/tickets">`, before the existing `<div className="space-y-4 sm:space-y-6">` inside the screen-only section (line ~910)

### 2. Contact info flex-column on mobile (line ~919)
- Current: `<div className="flex items-center gap-2 mt-1 text-sm text-[#64748b]">`
- Change to: `<div className="flex flex-col sm:flex-row sm:items-center gap-1 sm:gap-2 mt-1 text-sm text-[#64748b]">`
- Wrap contact name, email, and time in separate `<span>` elements so they stack vertically on mobile. Replace middots with line breaks on mobile, keep middots on desktop.

### 3. Reply card buttons flex-column on mobile (line ~1088)
- Current button row: `<div className="mt-4 flex items-center justify-between">`
- Change to: `<div className="mt-4 flex flex-col sm:flex-row sm:items-center gap-3">`
- The "Upload Attachment" area and the button group should each take full width on mobile.
- Make the button group also flex-col on mobile: `<div className="flex flex-col sm:flex-row items-stretch sm:items-center gap-2">`
- Each button needs `className="w-full sm:w-auto"` to fill width on mobile.

## Verification
- `npx tsc --noEmit`
- Manual: mobile view shows back button, contact info stacks vertically, reply buttons stack vertically and are full width
