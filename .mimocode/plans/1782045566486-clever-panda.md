# Assets Module: Add "Deleted" Status + "Assigned To (Dept)" Column

## Summary
Two changes to the Assets module: expand the status enum to include "Deleted", and add a new datatable column showing the assigned department name.

---

## Step 1: Mongoose Model — Add "Deleted" to enum
**File:** `lib/db/models/asset.ts`

- Line 14: Update `IAsset` status type union to include `"Deleted"`
- Line 62: Update `AssetSchema` status enum array to include `"Deleted"`

## Step 2: TypeScript Types — Add "Deleted" to all status unions
**File:** `lib/types/asset.ts`

- Line 16: `Asset.status` type union — add `"Deleted"`
- Line 36: `CreateAssetInput.status` type union — add `"Deleted"`

## Step 3: Service Layer — Add "Deleted" to status cast
**File:** `lib/services/asset-service.ts`

- Line 81: Update the `toAsset` status cast type union to include `"Deleted"`

## Step 4: Table Columns — Add "Deleted" status color + new "Assigned To (Dept)" column
**File:** `components/data-table/asset-data-table-columns.tsx`

- Line 18-24: Add `Deleted` entry to `statusConfig` (use rose/red styling like Items module)
- After the existing "Assigned To" column (line 160), add a new column:
  - `accessorKey: "assigned_to_department_name"`
  - Header: "Assigned To (Dept)"
  - Cell: shows department name or "N/A"

---

## Verification
1. Run `npx tsc --noEmit` — no type errors
2. Check the datatable shows the new "Assigned To (Dept)" column
3. Check the status dropdown in create/edit modals includes "Deleted" option
4. Check "Deleted" status renders with correct color in the table
