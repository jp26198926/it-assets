# Plan: Display Ticket Status Logs in Ticket History

## Goal
Fetch and display status logs in the "Ticket History" collapsible section on the ticket detail page.

## Changes

### 1. `lib/services/ticket-status-log-service.ts` — add getLogsByTicketId
Add function to fetch logs for a ticket, sorted by `created_at` desc. Populate `created_by` with user name.

### 2. `lib/actions/ticket-actions.ts` — add server action
Add `getTicketStatusLogs(ticketId)` action wrapping the service call.

### 3. `app/(dashboard)/tickets/[id]/page.tsx` — display logs
- Import `getTicketStatusLogs`
- Fetch logs in `useEffect` alongside ticket data
- Store in `statusLogs` state
- Render in the "Ticket History" `CollapsibleContent`:
  - Each log as a row with: `new_status` badge, `remarks`, `created_at` (formatted date), `created_by` name
  - Show "No history yet." if empty

## Files Modified
- `lib/services/ticket-status-log-service.ts`
- `lib/actions/ticket-actions.ts`
- `app/(dashboard)/tickets/[id]/page.tsx`

## Verification
1. Run `npm run dev`
2. Navigate to `/tickets/[id]`
3. Ticket History section shows log entries with status, remarks, date, and who made the change
