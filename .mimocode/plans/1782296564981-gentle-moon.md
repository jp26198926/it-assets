# Fix "Unknown Category" in By Category Chart

## Root Cause
`lib/services/ticket-report-service.ts` line 355: `$lookup` uses collection name `ticketcategories` but the actual MongoDB collection is `ticket_categories` (defined in `lib/db/models/ticket-category.ts` line 29).

## Fix
Change `from: "ticketcategories"` to `from: "ticket_categories"` on line 355 of `lib/services/ticket-report-service.ts`.

## Verification
- Lint the file
